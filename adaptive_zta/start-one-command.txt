Single-command run:

./run.sh

Notes:
- The script auto-selects the first free port starting from 8000.
- Override start port if needed: APP_PORT=8100 ./run.sh

UI:
- http://127.0.0.1:<selected-port>/ui
- http://127.0.0.1:<selected-port>/docs
